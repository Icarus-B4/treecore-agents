{
  "name": "@boilerplate/shell",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Reference app: proves the design system renders standalone.",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5180",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4180",
    "typecheck": "tsc -p . --noEmit"
  },
  "dependencies": {
    "@boilerplate/ui": "*",
    "react": "19.2.7",
    "react-dom": "19.2.7"
  }
}
